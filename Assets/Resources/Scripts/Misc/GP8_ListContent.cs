using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GP8_ListContent : MonoBehaviour
{
    public GP8_RESULTS gp8;

    public GP6_REVEALS_BASE gp6;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    internal void GiveRef(GP6_REVEALS_BASE gp6)
    {
        this.gp6 = gp6;

    }
}
